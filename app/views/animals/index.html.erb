<h1> Animais cadastrados </h1>

<% if @animals.any? %>

    <% @animals.each do |a| %>

        <div>
            <p><%= link_to a.name, animal_path(a.id) %> - <%= a.animal_type %> </p>
            
        </div>

    <% end %>
<% else %>
    <h2>Não há animais cadastrados!</h2>
<% end %>